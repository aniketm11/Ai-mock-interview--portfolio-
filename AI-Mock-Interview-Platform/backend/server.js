import express from 'express'; import helmet from 'helmet'; import cors from 'cors'; import cookieParser from 'cookie-parser'; import rateLimit from 'express-rate-limit'; import multer from 'multer'; import path from 'node:path'; import { fileURLToPath } from 'node:url'; import { z } from 'zod';
import env from '../config/env.js'; import { register, login } from '../services/auth-service.js'; import { authenticate, errorHandler } from '../middleware/security.js'; import { parseResume } from '../utils/resume-parser.js'; import { startInterview, submitAnswer, logIntegrity } from '../services/interview-service.js'; import { interviews } from '../models/repositories.js'; import { writeReport } from '../services/report-service.js';
const app=express(),root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),upload=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024}}),profile=z.object({role:z.string().min(2).max(100),level:z.enum(['junior','mid','senior']),skills:z.string().max(600),jobDescription:z.string().max(12000)}),answer=z.object({position:z.number().int().min(0),transcript:z.string().min(3).max(10000),voiceMetrics:z.object({pace:z.number().min(0).max(300),fillerCount:z.number().min(0),silenceSeconds:z.number().min(0),hesitationCount:z.number().min(0),confidence:z.number().min(0).max(100),fluency:z.number().min(0).max(100)})}),integrity=z.object({type:z.string().max(64),metadata:z.record(z.unknown()).optional()});
app.use(helmet({contentSecurityPolicy:false})); app.use(cors({origin:env.APP_ORIGIN,credentials:true})); app.use(express.json({limit:'1mb'})); app.use(cookieParser()); app.use('/api',rateLimit({windowMs:900000,limit:100}));
const session=(res,token)=>res.cookie('token',token,{httpOnly:true,sameSite:'lax',secure:env.NODE_ENV==='production',maxAge:604800000}).json({ok:true});
app.post('/api/auth/register',async(req,res,next)=>{try{session(res,await register(req.body.email?.toLowerCase(),req.body.password))}catch(e){next(e)}}); app.post('/api/auth/login',async(req,res,next)=>{try{session(res,await login(req.body.email?.toLowerCase(),req.body.password))}catch(e){next(e)}}); app.post('/api/auth/logout',(req,res)=>res.clearCookie('token').sendStatus(204));
app.post('/api/interviews',authenticate,upload.single('resume'),async(req,res,next)=>{try{const body=profile.parse(req.body);res.status(201).json(await startInterview(req.user.sub,{...body,resume:await parseResume(req.file)}))}catch(e){next(e)}}); app.get('/api/interviews',authenticate,async(req,res,next)=>{try{res.json(await interviews.list(req.user.sub))}catch(e){next(e)}}); app.post('/api/interviews/:id/answers',authenticate,async(req,res,next)=>{try{const interview=await interviews.owned(req.params.id,req.user.sub);if(!interview)throw Object.assign(new Error('Interview not found'),{status:404});const body=answer.parse(req.body);res.json(await submitAnswer(interview,body.position,body.transcript,body.voiceMetrics))}catch(e){next(e)}}); app.post('/api/interviews/:id/integrity-events',authenticate,async(req,res,next)=>{try{const interview=await interviews.owned(req.params.id,req.user.sub);if(!interview)throw Object.assign(new Error('Interview not found'),{status:404});res.status(201).json({severity:await logIntegrity(interview.id,integrity.parse(req.body))})}catch(e){next(e)}}); app.get('/api/interviews/:id/report.pdf',authenticate,async(req,res,next)=>{try{const interview=await interviews.owned(req.params.id,req.user.sub);if(!interview)throw Object.assign(new Error('Interview not found'),{status:404});await writeReport(res,interview)}catch(e){next(e)}});
app.use(express.static(path.join(root,'public')));app.use('/frontend',express.static(path.join(root,'frontend')));app.get('*',(_,res)=>res.sendFile(path.join(root,'public','index.html')));app.use(errorHandler);app.listen(env.PORT,()=>console.log(`AI Mock Interview Platform listening on ${env.PORT}`));
