import jwt from 'jsonwebtoken'; import env from '../config/env.js'; export function authenticate(req,res,next){try{req.user=jwt.verify(req.cookies.token||req.headers.authorization?.replace('Bearer ',''),env.JWT_SECRET);next()}catch{res.status(401).json({error:'Authentication required'})}} export function errorHandler(error,req,res,next){console.error({message:error.message,stack:error.stack});res.status(error.status||500).json({error:error.status?error.message:'Internal server error'})}
